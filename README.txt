1. XAMP se instala normalmente, --RECOMENDABLE LA INSTALACIÓN EN DISCO C(Disco de instalación por
	defecto de windows)
2. En caso de tener problemas en los puertos que xamp usa por defecto cambiar 	
	el puerto del archivo "C:\xampp\apache\conf\extra\httpd-ssl.conf" 
	--Especificamente en la línea 36 o aproximados
	-Cambiar Listen 443 por Listen 4430
3. Ejecutar el script database que en el proyecto viene. Paso por paso.
